name: ci

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
<<<<<<< HEAD
          pip install -r requirements.txt
          pip install pytest
      - name: Run tests
        run: python -m pytest mes-oee-analyzer -v
=======
          pip install -r operations-intelligence-analyzer/requirements.txt
          pip install pytest
      - name: Run tests
        run: python -m pytest operations-intelligence-analyzer -v
>>>>>>> 7037fd9 (Rebrand to operations-intelligence and restore parser/test compatibility)
